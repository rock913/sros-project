# Phase 4.2 生产环境优化与压力测试规划

**创建时间**: 2025-10-16 13:00  
**阶段**: Phase 4.2 Planning  
**前置条件**: Phase 4.1 LangFuse 集成完成  
**预计工期**: 2-3 天

---

## 🎯 Phase 4.2 目标

### 核心目标
在 Phase 4.1 可观测性基础设施之上，构建生产级性能和可靠性保障：

1. ✅ **性能基准测试**: 建立性能基线，识别瓶颈
2. ✅ **压力测试**: 验证系统在高负载下的稳定性
3. ✅ **性能优化**: 基于测试数据优化关键路径
4. ✅ **生产加固**: 安全、监控、容错机制完善

---

## 📋 任务分解

### P1 - 性能基准测试 (Day 1: 8 hours)

#### 1.1 测试场景设计 (2 hours)

**目标**: 定义代表性测试场景

**测试用例**:
| 场景 | 描述 | 预期耗时 | 关键指标 |
|------|------|---------|---------|
| **单用户研究流程** | 完整流程: 查询审批 → 论文检索 → 报告生成 → 修改审核 | < 60s | 端到端延迟 |
| **并发研究 (10 用户)** | 10 个用户同时发起研究 | < 120s | 吞吐量, 资源使用 |
| **HITL 响应延迟** | 从 HITL 请求到用户响应的平均时间 | 测量实际值 | P50, P95, P99 |
| **文档流式推送** | 1000 字报告的逐段生成速度 | < 10s | 首字节时间, 每段延迟 |
| **数据库查询性能** | Session/Paper/Report 查询耗时 | < 100ms | 查询延迟, 命中率 |

**交付物**:
- 测试用例文档: `backend/tests/performance/test_scenarios.md`
- 基准测试脚本: `backend/tests/performance/benchmark.py`

---

#### 1.2 性能测试框架搭建 (3 hours)

**技术选型**:
- **负载工具**: Locust (Python-based, 易于集成)
- **监控工具**: Prometheus + Grafana (实时可视化)
- **数据收集**: LangFuse (已集成), PostgreSQL slow query log

**框架结构**:
```
backend/tests/performance/
├── locustfile.py           # Locust 负载测试脚本
├── scenarios/              # 测试场景
│   ├── single_user.py      # 单用户测试
│   ├── concurrent_users.py # 并发用户测试
│   └── stress_test.py      # 压力测试
├── metrics/                # 指标收集
│   ├── prometheus.yml      # Prometheus 配置
│   └── grafana_dashboard.json  # Grafana 仪表板
└── reports/                # 测试报告
    └── baseline_YYYYMMDD.md
```

**交付物**:
- Locust 测试框架
- Prometheus/Grafana 配置
- 首次基准测试报告

---

#### 1.3 基准测试执行 (2 hours)

**测试流程**:
```bash
# 1. 启动监控
docker-compose -f docker-compose-monitoring.yml up -d

# 2. 运行 Locust 测试
cd backend/tests/performance
locust -f locustfile.py --host=http://localhost:8121

# 3. 访问 Locust UI: http://localhost:8089
#    - 设置用户数: 10
#    - 设置 spawn rate: 2/s
#    - 运行 5 分钟

# 4. 查看 Grafana: http://localhost:3001
#    - CPU 使用率
#    - 内存使用率
#    - 请求延迟 (P50, P95, P99)
#    - 错误率

# 5. 导出报告
```

**性能基线 (预期)**:
| 指标 | 目标值 | 说明 |
|------|--------|------|
| 单用户端到端延迟 (P95) | < 60s | 从发起到完成 |
| 并发吞吐量 (10 用户) | > 5 req/s | 每秒完成研究数 |
| LLM 调用延迟 (P95) | < 15s | Gemini API 响应 |
| 数据库查询延迟 (P95) | < 100ms | 单次查询 |
| CPU 使用率 (平均) | < 70% | 10 并发时 |
| 内存使用率 (平均) | < 2GB | 10 并发时 |

**交付物**:
- 性能基线报告: `baseline_20251016.md`
- Grafana 截图 (CPU, 内存, 延迟)
- LangFuse trace 样本 (10 个完整会话)

---

#### 1.4 瓶颈识别与分析 (1 hour)

**分析维度**:
1. **LLM 调用**: 是否是主要瓶颈? (预计占 70-80% 时间)
2. **数据库查询**: 是否有慢查询? 是否需要索引优化?
3. **文档生成**: diff 算法是否高效?
4. **WebSocket 推送**: 是否有延迟?

**分析工具**:
- LangFuse Dashboard: 查看各节点耗时分布
- PostgreSQL slow query log: 识别慢查询
- Python cProfile: 分析热点函数

**交付物**:
- 瓶颈分析报告: `bottleneck_analysis.md`
- 优化建议清单 (优先级排序)

---

### P2 - 压力测试 (Day 2: 6 hours)

#### 2.1 压力测试场景设计 (1 hour)

**目标**: 找到系统极限

**测试场景**:
| 场景 | 负载 | 持续时间 | 预期结果 |
|------|------|---------|---------|
| **渐增负载** | 1 → 50 用户 (每 30s +5) | 10 min | 找到性能拐点 |
| **峰值负载** | 100 并发用户 | 5 min | 验证极限吞吐量 |
| **持续负载** | 20 用户 | 30 min | 验证长时间稳定性 |
| **雪崩测试** | 突然从 0 → 100 用户 | 2 min | 验证快速扩容能力 |

**失败条件**:
- 错误率 > 5%
- P95 延迟 > 120s
- CPU 使用率 > 95% (持续 > 1 min)
- 内存溢出 (OOM)

---

#### 2.2 压力测试执行 (3 hours)

**执行步骤**:
```bash
# 1. 渐增负载测试
locust -f scenarios/stress_test.py --headless \
  --users 50 --spawn-rate 0.17 --run-time 10m

# 2. 峰值负载测试
locust -f scenarios/stress_test.py --headless \
  --users 100 --spawn-rate 10 --run-time 5m

# 3. 持续负载测试
locust -f scenarios/stress_test.py --headless \
  --users 20 --spawn-rate 2 --run-time 30m

# 4. 雪崩测试
locust -f scenarios/stress_test.py --headless \
  --users 100 --spawn-rate 50 --run-time 2m
```

**监控重点**:
- 实时 CPU/内存使用
- 错误率变化
- 数据库连接池状态
- WebSocket 连接数

---

#### 2.3 故障注入测试 (1 hour)

**目标**: 验证系统容错能力

**故障场景**:
1. **LLM API 失败**: 模拟 Gemini API 503 错误
2. **数据库连接断开**: 模拟 PostgreSQL 重启
3. **Redis 不可用**: 模拟缓存失效
4. **网络延迟**: 模拟 500ms 延迟

**测试方法**:
```bash
# 1. LLM API 失败 (临时禁用 API Key)
docker exec -it langgraph-api bash -c "export GEMINI_API_KEY=invalid"

# 2. 数据库断开
docker-compose -f docker-compose-dev.yml stop langgraph-postgres

# 3. Redis 不可用
docker-compose -f docker-compose-dev.yml stop langgraph-redis

# 4. 网络延迟 (使用 tc 命令)
docker exec -it langgraph-api tc qdisc add dev eth0 root netem delay 500ms
```

**预期行为**:
- ✅ 错误被捕获并记录到 LangFuse
- ✅ 返回用户友好的错误消息
- ✅ 系统自动重试 (可配置)
- ✅ 服务恢复后自动恢复

---

#### 2.4 压力测试报告 (1 hour)

**报告内容**:
1. 系统容量评估 (最大并发用户数)
2. 性能拐点分析 (何时性能开始下降)
3. 故障恢复能力评估
4. 资源使用建议 (CPU, 内存, 数据库连接池)

**交付物**:
- 压力测试报告: `stress_test_report_20251017.md`
- Grafana 截图 (各负载下的资源使用)
- 故障注入测试结果

---

### P3 - 性能优化 (Day 3: 8 hours)

#### 3.1 数据库优化 (2 hours)

**优化方向**:
1. **索引优化**:
   ```sql
   -- 为 Session 查询添加索引
   CREATE INDEX idx_session_thread_id ON sessions(thread_id);
   CREATE INDEX idx_session_created_at ON sessions(created_at);
   
   -- 为 Paper 查询添加索引
   CREATE INDEX idx_paper_session_id ON papers(session_id);
   
   -- 为 HITL 请求添加索引
   CREATE INDEX idx_hitl_request_id ON hitl_requests(request_id);
   CREATE INDEX idx_hitl_session_id ON hitl_requests(session_id);
   ```

2. **查询优化**:
   - 使用 `SELECT DISTINCT` 代替多次查询
   - 添加查询缓存 (Redis)
   - 批量插入代替逐条插入

3. **连接池配置**:
   ```python
   # 增大连接池
   engine = create_engine(
       DATABASE_URL,
       pool_size=20,  # 从 10 增加到 20
       max_overflow=40  # 从 20 增加到 40
   )
   ```

**交付物**:
- 数据库迁移脚本: `migrations/002_add_performance_indexes.sql`
- 查询优化建议文档

---

#### 3.2 LLM 调用优化 (2 hours)

**优化方向**:
1. **提示词优化**:
   - 减少 prompt 长度 (保持质量)
   - 使用更精简的系统提示词

2. **批量调用** (如适用):
   - 将多个小请求合并为一个大请求

3. **缓存策略**:
   ```python
   # 对相同研究主题的结果缓存 1 小时
   @cached(ttl=3600, key="research_report:{research_topic}")
   def generate_report(research_topic):
       # ...
   ```

4. **超时控制**:
   ```python
   response = completion(
       model=cfg.generation_model,
       messages=messages,
       timeout=30,  # 30 秒超时
       max_retries=2  # 最多重试 2 次
   )
   ```

**交付物**:
- 提示词优化版本
- 缓存策略实现
- 性能对比报告 (优化前后)

---

#### 3.3 WebSocket 优化 (2 hours)

**优化方向**:
1. **文档更新批量化**:
   ```python
   # 每 2 秒或累计 5 个变更时才推送
   if time.time() - last_push > 2.0 or len(pending_diffs) >= 5:
       await websocket.send_json({
           "type": "document_update_batch",
           "diffs": pending_diffs
       })
       pending_diffs = []
   ```

2. **压缩传输**:
   ```python
   # 使用 gzip 压缩大型 payload
   import gzip
   compressed_data = gzip.compress(json.dumps(data).encode())
   await websocket.send_bytes(compressed_data)
   ```

3. **心跳优化**:
   ```python
   # 增加心跳间隔 (30s → 60s)
   HEARTBEAT_INTERVAL = 60
   ```

**交付物**:
- WebSocket 优化代码
- 性能对比报告

---

#### 3.4 缓存策略实现 (2 hours)

**缓存层级**:
```
L1: 内存缓存 (functools.lru_cache)
    └── 热点函数结果 (TTL: 5 min)

L2: Redis 缓存
    ├── 研究报告 (TTL: 1 hour)
    ├── 论文摘要 (TTL: 24 hours)
    └── arXiv 搜索结果 (TTL: 6 hours)

L3: PostgreSQL 持久化
    └── 所有历史数据 (永久)
```

**实现示例**:
```python
import redis
import json

redis_client = redis.Redis(host='langgraph-redis', port=6379, db=0)

def get_cached_report(research_topic: str) -> Optional[str]:
    cache_key = f"report:{research_topic}"
    cached = redis_client.get(cache_key)
    if cached:
        return json.loads(cached)
    return None

def set_cached_report(research_topic: str, report: str, ttl: int = 3600):
    cache_key = f"report:{research_topic}"
    redis_client.setex(cache_key, ttl, json.dumps(report))
```

**交付物**:
- 缓存工具模块: `backend/src/agent/cache.py`
- 缓存策略文档

---

### P4 - 生产加固 (Day 3 下午: 4 hours)

#### 4.1 安全加固 (1 hour)

**措施**:
1. **环境变量验证**:
   ```python
   # 启动时验证必需的环境变量
   required_vars = ["GEMINI_API_KEY", "LANGFUSE_PUBLIC_KEY", "LANGFUSE_SECRET_KEY"]
   for var in required_vars:
       if not os.getenv(var):
           raise EnvironmentError(f"Missing required environment variable: {var}")
   ```

2. **输入验证**:
   ```python
   # 验证研究主题长度
   if len(research_topic) > 500:
       raise ValueError("Research topic too long (max 500 chars)")
   ```

3. **速率限制**:
   ```python
   from slowapi import Limiter
   limiter = Limiter(key_func=get_remote_address)
   
   @app.post("/agent/invoke")
   @limiter.limit("10/minute")  # 每分钟最多 10 次请求
   async def invoke_agent(request: AgentInvokeRequest):
       # ...
   ```

**交付物**:
- 安全加固代码
- 安全检查清单

---

#### 4.2 监控告警 (1 hour)

**告警规则**:
| 指标 | 阈值 | 动作 |
|------|------|------|
| 错误率 | > 5% (5 min) | 邮件 + Slack |
| P95 延迟 | > 120s (5 min) | Slack |
| CPU 使用率 | > 90% (2 min) | 邮件 |
| 内存使用率 | > 85% | 邮件 |
| 数据库连接池 | > 80% | Slack |

**实现方式**:
- Prometheus AlertManager
- LangFuse Webhook → Slack
- Email 通知 (SMTP)

**交付物**:
- AlertManager 配置: `monitoring/alertmanager.yml`
- Slack Webhook 集成代码

---

#### 4.3 容错机制 (1 hour)

**重试策略**:
```python
from tenacity import retry, stop_after_attempt, wait_exponential

@retry(
    stop=stop_after_attempt(3),
    wait=wait_exponential(multiplier=1, min=2, max=10)
)
def call_llm_with_retry(prompt: str):
    return completion(model=cfg.generation_model, messages=[...])
```

**降级策略**:
```python
# LLM API 失败时返回简化报告
try:
    report = call_llm_with_retry(prompt)
except Exception as e:
    logger.error(f"LLM API failed: {e}")
    report = "Failed to generate report due to API error. Please try again later."
    # 记录到 LangFuse
    trace.update(status="degraded", status_message=str(e))
```

**断路器**:
```python
from pybreaker import CircuitBreaker

llm_breaker = CircuitBreaker(fail_max=5, timeout_duration=60)

@llm_breaker
def call_llm(prompt: str):
    # ...
```

**交付物**:
- 容错机制代码
- 降级策略文档

---

#### 4.4 健康检查 (1 hour)

**健康检查端点**:
```python
@app.get("/health", tags=["System"])
async def health_check():
    """
    Health check endpoint for load balancers and monitoring.
    
    Returns:
        200: Service is healthy
        503: Service is unhealthy
    """
    checks = {
        "database": check_database_connection(),
        "redis": check_redis_connection(),
        "llm_api": check_llm_api_availability(),
        "langfuse": check_langfuse_connection()
    }
    
    all_healthy = all(checks.values())
    status_code = 200 if all_healthy else 503
    
    return JSONResponse(
        status_code=status_code,
        content={
            "status": "healthy" if all_healthy else "unhealthy",
            "checks": checks,
            "timestamp": datetime.utcnow().isoformat()
        }
    )
```

**Kubernetes Readiness Probe**:
```yaml
readinessProbe:
  httpGet:
    path: /health
    port: 8000
  initialDelaySeconds: 10
  periodSeconds: 5
```

**交付物**:
- 健康检查端点实现
- Kubernetes probe 配置示例

---

## 📊 Phase 4.2 交付清单

### 文档交付

- [ ] 性能基准测试报告
- [ ] 压力测试报告
- [ ] 瓶颈分析报告
- [ ] 优化建议文档
- [ ] 安全检查清单
- [ ] 监控告警配置文档

### 代码交付

- [ ] Locust 测试框架 (`backend/tests/performance/`)
- [ ] 数据库索引优化 (`migrations/002_*.sql`)
- [ ] 缓存工具模块 (`backend/src/agent/cache.py`)
- [ ] 安全加固代码 (输入验证, 速率限制)
- [ ] 容错机制 (重试, 降级, 断路器)
- [ ] 健康检查端点 (`/health`)

### 配置交付

- [ ] Prometheus/Grafana 配置
- [ ] AlertManager 告警规则
- [ ] Kubernetes probe 配置示例

### 测试数据

- [ ] 性能基线数据 (Grafana JSON)
- [ ] 压力测试结果 (Locust HTML 报告)
- [ ] LangFuse trace 样本 (100+ traces)

---

## 🎯 成功标准

### 性能指标

| 指标 | Phase 4.1 基线 | Phase 4.2 目标 | 提升 |
|------|---------------|---------------|------|
| 单用户延迟 (P95) | ~60s | < 50s | 16% ↑ |
| 并发吞吐量 (10 用户) | ~5 req/s | > 8 req/s | 60% ↑ |
| LLM 调用延迟 (P95) | ~15s | < 12s | 20% ↑ |
| 数据库查询 (P95) | ~100ms | < 50ms | 50% ↑ |
| 错误率 (正常负载) | < 2% | < 1% | 50% ↓ |

### 可靠性指标

| 指标 | 目标值 |
|------|--------|
| 最大并发用户数 | > 50 |
| 持续负载稳定性 (30 min) | 0 崩溃 |
| 故障恢复时间 | < 30s |
| 健康检查响应时间 | < 100ms |

---

## 📅 执行时间表

| 日期 | 任务 | 负责人 | 状态 |
|------|------|--------|------|
| **Day 1 (Oct 17)** | P1.1-1.4: 性能基准测试 | GitHub Copilot | 📋 Planned |
| **Day 2 (Oct 18)** | P2.1-2.4: 压力测试 | GitHub Copilot | 📋 Planned |
| **Day 3 上午 (Oct 19)** | P3.1-3.4: 性能优化 | GitHub Copilot | 📋 Planned |
| **Day 3 下午 (Oct 19)** | P4.1-4.4: 生产加固 | GitHub Copilot | 📋 Planned |
| **Day 4 (Oct 20)** | 最终验证 + 文档整理 | GitHub Copilot | 📋 Planned |

---

## 🔮 Phase 4.3 前瞻

**可能的方向**:
1. **多模型支持优化**: GPT-4, Claude, Gemini 性能对比和成本优化
2. **A/B 测试框架**: 基于 LangFuse 实现不同策略的对比测试
3. **自动扩容**: Kubernetes HPA (Horizontal Pod Autoscaling)
4. **成本优化**: Token 使用监控和优化建议

---

**规划创建时间**: 2025-10-16 13:00  
**规划版本**: v1.0  
**负责人**: GitHub Copilot  
**审核**: 待用户确认

---

*本规划遵循 GEMINI.md 文档规范，基于 Phase 4.1 可观测性基础设施*
