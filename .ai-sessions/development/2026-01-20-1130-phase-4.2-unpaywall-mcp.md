# Session: Phase 4.2 - Tool Migration (Unpaywall)

**Date:** 2026-01-20
**Architect:** GitHub Copilot
**Phase:** 4.2 (MCP Infrastructure & Hexagonal Migration)

## Objective
Migrate the `Unpaywall` tool to the MCP ecosystem by creating an `McpTool` wrapper.

## Architecture
- **Source Logic**: `UnpaywallAdapter` (implements `PaperFetcher`).
- **Target Format**: `McpTool` (Schema + Handler).
- **Location**: `backend/src/agent/infrastructure/mcp/tools/unpaywall.py`.

## Design Pattern
"Adapter of an Adapter":
1. The `UnpaywallAdapter` adapts the external library (`unpywall`) to our Domain (`PaperFetcher`).
2. The `UnpaywallMcpTool` adapts our Domain (`PaperFetcher`) to the MCP Infrastructure (`McpTool`).

## Task
1. Create `backend/src/agent/infrastructure/mcp/tools/unpaywall.py`.
2. Define `get_unpaywall_mcp_tool() -> McpTool`.
3. The handler should properly accept arguments (e.g. `doi`) and return the JSON result.

## Progress
- [x] Create McpUnpaywallAdapter (`backend/src/agent/infrastructure/mcp/tools/unpaywall.py`)
- [x] Fix Import paths and verify with tests.
- [x] Create Entrypoint (`backend/src/agent/infrastructure/mcp/entrypoint.py`)
- [x] Verification: Smoke Test Passed (Server starts and registers tool).

## Conclusion
Phase 4.2 Initial Setup is complete. We have a working MCP Server scaffolding and one migrated tool.
Future work involves migrating the rest of the tools (Arxiv, Zotero) and deploying this via the VS Code Extension.
