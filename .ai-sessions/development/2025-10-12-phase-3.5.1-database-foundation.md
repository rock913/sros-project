# Phase 3.5.1: 数据库基础与会话管理

## 会话元数据

- **日期**: 2025-10-12
- **开发者**: AI Assistant
- **任务**: Phase 3.5.1 - Database Foundation & Session Management
- **优先级**: P0 (核心功能)
- **预计时长**: Week 1-2 of Phase 3.5
- **依赖**: Checkpointer 已完成 ✅

## 任务目标

实现完整的数据库持久化层，支持会话管理、论文收藏、报告存储和事件追踪。

### 核心交付物

1. **数据库架构设计**
   - 4 张核心表：sessions, papers, reports, session_events
   - SQLAlchemy ORM 模型
   - 数据库迁移脚本（Alembic）

2. **会话管理 API**
   - 5 个 RESTful 端点（CRUD + Events）
   - 与 LangGraph Checkpointer 集成
   - 自动会话持久化

3. **VS Code 扩展集成**
   - SessionsProvider TreeView
   - 右键菜单（Load Session, Delete Session）
   - 会话状态同步

## 开发计划

### Phase 1: 数据库架构设计 (2h)

- [ ] 设计 PostgreSQL Schema
- [ ] 创建 SQLAlchemy 模型
- [ ] 添加 Alembic 迁移支持
- [ ] 编写数据库初始化脚本

### Phase 2: Session Management API (3h)

- [ ] 实现 5 个 Session API 端点
- [ ] 与 LangGraph thread_id 关联
- [ ] 添加单元测试 (5个测试用例)
- [ ] 更新 openapi.yaml

### Phase 3: VS Code 扩展集成 (4h)

- [ ] 创建 SessionsProvider TreeView
- [ ] 实现会话加载功能
- [ ] 添加右键菜单交互
- [ ] 测试前后端联调

---

## 详细步骤记录

### Step 1: 数据库架构设计

#### 1.1 分析需求

根据 ROADMAP.md Phase 3.5.1 要求：

**核心表设计**：
```sql
-- 1. sessions: 研究会话元数据
CREATE TABLE sessions (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    thread_id UUID NOT NULL UNIQUE,  -- 关联 LangGraph checkpoints
    title VARCHAR(500) NOT NULL,
    research_topic TEXT,
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW(),
    status VARCHAR(50) DEFAULT 'active',  -- active, completed, archived
    tags TEXT[],  -- 标签数组
    notes TEXT
);

-- 2. papers: 收藏的论文
CREATE TABLE papers (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    session_id UUID REFERENCES sessions(id) ON DELETE CASCADE,
    title VARCHAR(1000) NOT NULL,
    authors TEXT[],
    abstract TEXT,
    doi VARCHAR(255),
    arxiv_id VARCHAR(50),
    url TEXT,
    created_at TIMESTAMP DEFAULT NOW(),
    metadata JSONB  -- 扩展字段
);

-- 3. reports: 生成的研究报告
CREATE TABLE reports (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    session_id UUID REFERENCES sessions(id) ON DELETE CASCADE,
    content TEXT NOT NULL,
    format VARCHAR(50) DEFAULT 'markdown',  -- markdown, pdf, html
    version INT DEFAULT 1,
    created_at TIMESTAMP DEFAULT NOW(),
    metadata JSONB
);

-- 4. session_events: 会话事件日志
CREATE TABLE session_events (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    session_id UUID REFERENCES sessions(id) ON DELETE CASCADE,
    event_type VARCHAR(100) NOT NULL,  -- query_generated, paper_retrieved, report_created
    event_data JSONB,
    created_at TIMESTAMP DEFAULT NOW()
);

-- 索引
CREATE INDEX idx_sessions_thread_id ON sessions(thread_id);
CREATE INDEX idx_sessions_status ON sessions(status);
CREATE INDEX idx_papers_session_id ON papers(session_id);
CREATE INDEX idx_reports_session_id ON reports(session_id);
CREATE INDEX idx_events_session_id ON session_events(session_id);
CREATE INDEX idx_events_type ON session_events(event_type);
```

**设计决策**：
- ✅ 使用 UUID 作为主键（与 LangGraph checkpointer 一致）
- ✅ `thread_id` 作为外键关联 LangGraph 状态
- ✅ JSONB 存储扩展元数据（灵活性）
- ✅ 级联删除（数据一致性）
- ✅ 时间戳审计（created_at, updated_at）

#### 1.2 创建 SQLAlchemy 模型

开始实现...

